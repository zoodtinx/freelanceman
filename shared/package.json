{
  "name": "freelanceman-common",
  "version": "1.0.0",
  "main": "dist/schemas/index.js",
  "types": "dist/types/schemas/index.d.ts",
  "type": "commonjs",
  "scripts": {
    "clean": "rm -rf ./dist",
    "build": "rm -rf tsconfig.tsbuildinfo && npm run clean && tsc --build"
  },
  "dependencies": {
    "g": "^2.0.1",
    "ts-prune": "^0.10.3",
    "zod": "^3.25.67"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
