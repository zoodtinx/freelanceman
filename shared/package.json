{
  "name": "shared",
  "version": "1.0.0",
  "main": "",
  "types": "./types/index.d.ts",
  "scripts": {
    "clean": "rm -rf **/*.js **/*.d.ts **/*.js.map",
    "build": "npm run clean && tsc --build"
  }
}