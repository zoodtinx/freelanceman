import mockFiles from './files';

const mockBrief = {
   id: 'brief-001',
   brief: 'The files array contains seven entries representing various documents associated with different client projects. Each entry includes a unique identifier (id), the file name (e.g., "Project Proposal.pdf," "Invoice_September.docx"), its size (ranging from 200 KB to 3.5 MB), a link to access the file, the associated project name (e.g., "Client Project A"), and the corresponding client (e.g., "Client A"). This structured data is essential for managing and retrieving project-related documents efficiently in the FreelanceMan application.',
   materials: mockFiles,
};

export default mockBrief;