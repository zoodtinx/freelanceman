// import * as projects from './base/projects';
// import * as clientContacts from './contacts/client-contacts';

// export const generateProjects = (userId: string, clientId: string) => {
//     const projectGenerators = Object.values(projects);

//     return projectGenerators.flatMap((generateFn) =>
//         generateFn(userId, clientId),
//     );
// };

// export const generateClientContacts = (userId: string, clientId: string) => {
//     const contactGenerators = Object.values(clientContacts);

//     return contactGenerators.flatMap((generateFn) =>
//         generateFn(userId, clientId),
//     );
// };
